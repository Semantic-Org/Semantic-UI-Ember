<div class="ui masthead vertical segment">
  <div class="ui container">
    <div class="introduction">

      <h1 class="ui header">
        Dropdown

        <div class="sub header">
          A dropdown allows a user to select a value from a series of options
        </div>
      </h1>

      <div class="ui hidden divider"></div>

      <p>
        For additional documentation on how to configure this module, please refer to the <a href="/">Getting Started</a> section and Semantic UI's module specific external documentation links below.
      </p>

      <div class="ui four item stackable tabs menu">
        <a class="item" href="http://semantic-ui.com/modules/dropdown.html" target="_blank">Definition</a>
        <a class="item" href="http://semantic-ui.com/modules/dropdown.html#/examples" target="_blank">Examples</a>
        <a class="item" href="http://semantic-ui.com/modules/dropdown.html#/usage" target="_blank">Usage</a>
        <a class="item" href="http://semantic-ui.com/modules/dropdown.html#/settings" target="_blank">Settings</a>
      </div>

    </div>

  </div>
</div>

<div class="ui main container">

  <div class="ui grid">
    <div class="row">
      <div class="eight wide column">
        <h4 class="ui header">
          Selection
          <div class="sub header">A dropdown can be used to select between choices in a form</div>
        </h4>

        {{#ui-dropdown class="search selection" allowAdditions=true selected="Bob" onChange=(action (mut gender))}}
          <div class="default text">Gender</div>
          <i class="dropdown icon"></i>
          <div class="menu">
            {{#each genders as |gender|}}
              <div class="item" data-value={{gender.id}}>
                {{gender.text}}
              </div>
            {{/each}}
          </div>
        {{/ui-dropdown}}
      </div>
      <div class="eight wide column">
        <div class="ui form">
          <div class="ui field">
            <label>Selected</label>
            {{input value=gender readonly="true"}}
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="eight wide column">
        <h4 class="ui header">
          Selection
          <div class="sub header">A dropdown can be used to select between choices in a form</div>
        </h4>

        {{#ui-dropdown class="search selection" allowAdditions=true selected=selected_type onChange=(action (mut selected_type)) as |execute mapper|}}
          <div class="default text">Types</div>
          <i class="dropdown icon"></i>
          <div class="menu">
            <div class="item" data-value="">All Items</div>
            {{#each types as |type|}}
              <div class="item" data-value={{map-value mapper type}}>
                {{type}}
              </div>
            {{/each}}
          </div>
        {{/ui-dropdown}}
      </div>
      <div class="eight wide column">
        <div class="ui form">
          <div class="ui field">
            <label>Selected</label>
            {{input value=selected_type readonly="true"}}
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="eight wide column">
        <h4 class="ui header">
          Selection
          <div class="sub header">A dropdown can be used to select between choices in a form</div>
        </h4>

        {{#ui-dropdown class="selection" selected=gender2 onChange=(action (mut gender2)) as |execute mapper|}}
          <div class="default text">Gender</div>
          <i class="dropdown icon"></i>
          <div class="menu">
            {{#each genders as |gender|}}
              <div class="item" data-value={{map-value mapper gender}}>
                {{gender.text}}
              </div>
            {{/each}}
          </div>
        {{/ui-dropdown}}
      </div>
      <div class="eight wide column">
        <div class="ui form">
          <div class="ui field">
            <label>Selected</label>
            {{input value=gender2.text readonly="true"}}
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="eight wide column">
        <h4 class="ui header">
          Multi Select
          <div class="sub header">A dropdown can be used to select between choices in a form</div>
        </h4>

        {{#ui-dropdown class="fluid multiple selection" selected=country onChange=(action (mut country))}}
          <i class="dropdown icon"></i>
          <div class="default text">Select Country</div>
          <div class="menu">
            {{#each countries as |item|}}
              <div class="item" data-value={{item.iso2}}>
                <i class="{{item.iso2}} flag"></i>
                {{item.name}}
              </div>
            {{/each}}
          </div>
        {{/ui-dropdown}}
      </div>
      <div class="eight wide column">
        <div class="ui form">
          <div class="ui field">
            <label>Selected</label>
            {{input value=country readonly="true"}}
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="eight wide column">
        <h4 class="ui header">
          Multi Select Binded
          <div class="sub header">A dropdown can be used to select between choices in a form</div>
        </h4>

        {{#ui-dropdown class="fluid multiple selection" selected=countries onChange=(action (mut country2)) as |execute mapper|}}
          <i class="dropdown icon"></i>
          <div class="default text">Select Country</div>
          <div class="menu">
            {{#each countries as |item|}}
              <div class="item" data-value={{map-value mapper item}}>
                <i class="{{item.iso2}} flag"></i>
                {{item.name}}
              </div>
            {{/each}}
          </div>
        {{/ui-dropdown}}
      </div>
      <div class="eight wide column">
        <div class="ui form">
          <div class="ui field">
            <label>Selected</label>
            {{input value=country2.length readonly="true"}}
          </div>
        </div>
      </div>
    </div>
  </div>

</div>
